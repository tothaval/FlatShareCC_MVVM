<UserControl x:Class="WGMietkosten.Views.FlatSetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WGMietkosten.Views"            
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="StackPanel">
            <Setter Property="Margin" Value="0 5"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="MinWidth" Value="300"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="MinWidth" Value="100"/>
        </Style>
    </UserControl.Resources>
    
    <Grid HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>            
        </Grid.RowDefinitions>

        <TextBlock Text="Flat Setup"
                   FontSize="24"
                   Grid.Row="0"/>

        <StackPanel Orientation="Horizontal"
                      Grid.Row="1">
            <TextBlock Text="Address"/>
            <TextBox Grid.Column="1"
                     Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal"
                      Grid.Row="2">
            <TextBlock Text="Details"/>
            <TextBox Text="{Binding Details, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal"
                      Grid.Row="3">
            <TextBlock Text="Area"/>
            <TextBox Text="{Binding Area, UpdateSourceTrigger=LostFocus}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal"
                      Grid.Row="4">
            <TextBlock Text="Rooms"/>
            <TextBox Text="{Binding Rooms, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal"
                      Grid.Row="5">
            <TextBlock Text="Tenants"/>
            <TextBox Text="{Binding Tenants, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <Grid Grid.Row="6" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" SharedSizeGroup="left"/>
                <ColumnDefinition Width="*" SharedSizeGroup="right"/>
            </Grid.ColumnDefinitions>
            <Button Content="finish setup"
                    Margin="10 0 10 0"
                    Grid.Column="0"
                    Command="{Binding FinishSetupCommand}"
                    />
            <Button Content="cancel setup"
                    Margin="10 0 10 0"
                    Grid.Column="1"
                    Command="{Binding CancelSetupCommand}"
                    />

        </Grid>
    </Grid>
</UserControl>
