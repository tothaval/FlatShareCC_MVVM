<UserControl x:Class="SharedLivingCostCalculator.Views.FlatSetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SharedLivingCostCalculator.Views"
             xmlns:viewmodels="clr-namespace:SharedLivingCostCalculator.ViewModels"
             xmlns:c_views="clr-namespace:SharedLivingCostCalculator.Components.ComponentViews"
             d:DataContext="{d:DesignInstance Type=viewmodels:FlatSetupViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >
    <UserControl.Resources>
        <Style TargetType="c_views:SimpleInputView">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Padding" Value="2"/>
        </Style>
        <Style TargetType="ListView">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
    </UserControl.Resources>
    
    <Grid HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"
          Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Flat Setup" FontSize="24" Grid.Row="0"
                   HorizontalAlignment="Stretch"
                   TextAlignment="Center"/>

        <c_views:SimpleInputView Label="Address"
                                 Value="{Binding FlatSetup.Address, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                 Foreground="DarkSlateGray"
                                 Grid.Row="1"/>

        <c_views:SimpleInputView Label="Details"
                                 Value="{Binding FlatSetup.Details, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                 Foreground="DarkSlateGray"
                                 Grid.Row="2"/>

        <c_views:SimpleInputView Label="Area"
                                 Value="{Binding FlatSetup.Area, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                 Foreground="DarkSlateGray"
                                 Grid.Row="3"/>
        
        <c_views:SimpleInputView Label="Rooms"
                                 Value="{Binding FlatSetup.Rooms, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                 Foreground="DarkSlateGray"
                                 Grid.Row="4"/>


            <ListView ItemsSource="{Binding FlatSetup.rooms}"
                  SelectionMode="Single" Grid.Row="5"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      BorderThickness="0"
                      Margin="0 5 0 0"
                      >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="FloralWhite">
                            <WrapPanel Orientation="Vertical" Margin="0 5">
                            <c_views:SimpleInputView Label="{Binding ID, StringFormat='Room {0}'}"
                                 Value="{Binding RoomName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                 Foreground="DarkSlateGray" Padding="5 0"/>

                            <c_views:SimpleInputView Label="Area"
                                 Value="{Binding RoomArea, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                 Foreground="DarkSlateGray" Padding="5 0"/>
                        </WrapPanel>
                    </Border>
                    </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>

        <TextBlock Grid.Row="6" />

        <Button Grid.Row="7"
                HorizontalAlignment="Stretch"
                
                Content="Proceed"
                Command="{Binding FlatSetupCommand}"/>

        <Button Grid.Row="8"
                HorizontalAlignment="Stretch"
                
                Content="Leave"
                Command="{Binding LeaveViewCommand}"/>

    </Grid>
</UserControl>
